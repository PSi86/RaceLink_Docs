# MkDocs Material — minimal core
mkdocs>=1.6
mkdocs-material>=9.5
pymdown-extensions>=10.7

# Lightbox for clickable screenshots (prev/next, keyboard, touch, zoom).
mkdocs-glightbox>=0.4

# Recommended extras (uncomment as needed; matching plugin entries
# also need to be enabled in mkdocs.yml):
#
# mkdocs-git-revision-date-localized-plugin>=1.2
# mkdocs-htmlproofer-plugin>=1.2
# mkdocs-redirects>=1.2
# mike>=2.1                  # versioned docs deployment
